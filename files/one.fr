enum Direction {
    Right = 1,
    Left,
    Up,
    Down
}

struct Location {
    x: int,
    y: int
}

struct AppData {
    title: string,
    maxAge: int,
    dirs: [Direction]?,
    a: (string, Direction, int)
}